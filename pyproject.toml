[tool.pytest.ini_options]
minversion = "6.0"
addopts = "--strict-markers --maxfail=1 -q --cov=src --cov-branch --cov-report=term-missing --cov-fail-under=80"
testpaths = ["tests"]
markers = [
    "performance: marks tests as performance benchmarks (deselect with '-m \"not performance\"')",
]
